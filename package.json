{
  "name": "agentic-tracker",
  "private": true,
  "description": "Agentic-coding model capability and price tracker. Version lives in VERSION, not here.",
  "type": "module",
  "scripts": { "test": "node --test test/*.test.mjs" },
  "devDependencies": { "jsdom": "25.0.1" }
}
